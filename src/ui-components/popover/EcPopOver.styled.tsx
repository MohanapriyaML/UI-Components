import { Popover } from "@mui/material";
import styled from "styled-components";

export const PopOverContainer = styled(Popover)`
    float:center;
`
export const PopOverTypo = styled('div')`
   height:400px;
   width:400px;
   display:flex;
   justify-content:center;
`;